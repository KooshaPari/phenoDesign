# Asset pipeline (moved)

The render pipeline formerly under `engine/` lives in the canonical peer repo:

**[`KooshaPari/asset-engine`](https://github.com/KooshaPari/asset-engine)**

Do not re-add Blender, FFmpeg, ImageMagick, or orchestrator scripts here. phenoDesign owns design tokens and UX; asset-engine owns headless renders.
